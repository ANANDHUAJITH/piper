# Python dependencies for PIPER Arm Lab
# Install with: pip3 install -r requirements.txt

numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.4.0

# piper_sdk is installed separately from the manufacturer.
# It is NOT available on PyPI.
# Follow the manufacturer installation instructions before running.
